---
description: Standards for Backend development using Ox, Tapir, and jOOQ
globs: ["modules/backend/**/*.scala"]
---

# Backend Standards (Ox, Tapir, jOOQ)

## Concurrency with Ox
- Prefer **direct style** over Monadic (F[_]) styles.
- Use `scoped` and `fork` for structured concurrency.
- Leverage `Ox` channels for communication between fibers.
- Use `retry` and `timeout` utilities provided by Ox.

## API with Tapir
- Define endpoints separately from business logic.
- Use `endpoint` to describe the API and `ServerInterpreter` to implement it.
- Ensure all endpoints are documented (OpenAPI/Swagger).

## Database with jOOQ
- Use jOOQ's typesafe DSL for all queries.
- Keep SQL logic within repository layers.
- Ensure database transactions are handled correctly, ideally using Ox-friendly patterns.
- Follow a "Schema-first" approach: update SQL migrations and then regenerate jOOQ classes.
